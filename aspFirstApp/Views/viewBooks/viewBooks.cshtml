@using aspFirstApp.Models

@{
    ViewBag.Title = "Home";
}

@{
    Layout = "~/Views/Shared/AdminLayout.cshtml";
}
@{
    if (Session["id"] == null)
    {
        Response.Redirect("~/Login/Index");

    }
}
<script src="../Scripts/jquery-1.10.2.js"></script>
<script>

    $(document).ready(function () {
        $("#ab").keyup(function () {
            
            
            vals = $("#result");
            val1 = $("#ab").val();
            
            $.getJSON("/viewBooks/get?val=" + val1, function (data) {
                
              // $.each(data, function (index, item) {
                
                vals.text("Search Result " + data);
            // });
            });
        });

       












    });
</script>

<div class="col-lg-8 col-lg-offset-2" style="margin-top:120px;" id="i">
    <div class="panel panel-success">
        <div class="panel-heading">
            <h3 class="panel-title">VIEW BOOKS
        </div>
        <div class="panel-body">
            <h1 id="result"></h1>
            
            <div id="result"></div>
            <form role="form" class="form-horizontal col-lg-offset-8" name="vinform">
                <div class="form-group " id="c">
                    <label for="t1" class="col-lg-2 control-label"></label>
                    <div class=" input-group">
                        <input type="text" class="form-control" id="ab" name="t1"   placeholder="Search books">
                        <span class="input-group-addon">
                            <i class="fa fa-search"></i>
                        </span>
                    </div>
                </div>


        </div>
        </form>
        <div class="table-responsive margin" style="width: 800px; margin-left: 20px"id="an">
            <table class="table  table-hover table-hover" id="tb">
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>Name</th>
                        <th>ISBN</th>
                        <th>Category</th>
                        
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    <span id="res"></span>
                                        @{
    foreach (var book in Model)
    {
        <tr height="40">
            <td>@book.Id</td>
            <td>@book.cname</td>
            <td>@book.isbn</td>
            <td>@book.Category.name</td>
            <td>
                <!--<a href='?p=<%=r.getInt(1)%>' class='btn btn-primary'>Request</a>
                <form action="/viewBooks/delete" method="post">
                    <input type="hidden" name="p" value=@book.Id />
                    <input type="hidden" name="p1" value=@book.catId />
                    <input type="submit" class="btn btn-success" value="DELETE">
                                        </form>
                                        -->
                
            </td>
            <td>
                <form action="/viewBooks/update" method="post">
                    <input type="hidden" name="p" value=@book.Id />
                    <input type="hidden" name="p1" value=@book.catId />
                    <input type="submit" class="btn btn-default" value="UPDATE">
                </form>
            </td>
           
            
        </tr>
    }
}




                </tbody>
            </table>
        
        </div>
    </div>
</div>

